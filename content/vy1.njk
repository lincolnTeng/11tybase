---
permalink: /vy1/
layout: layouts/home.njk
eleventyNavigation:
  key: Videoyear
  order: 6
---

   start rendering....<br>
    <p>  o count  </p> 
 	{% for vdaykey,vidlist  in  vMap %}
			 <ul>
			  <li>   the day : {{vdaykey }} and count:no count   </li>
			
  
            {% for  vid  in  vidlist %}
        				  
                        <li>       <h3>{{ videoInfo[ vid].title }}   the vid : {{vid }} =</h3>    </li>
             
                      {% set vUrl %}/videoitem/{{ videoInfo[vid].videoid | slugify }}/{% endset %}
                    	<li><a href="{{ vUrl }}" class="post-tag">src of {{ videoInfo[vid].title }}</a></li>
						
            {% endfor %}
				  
				    
				  </ul>
     {% endfor %}
end of rending ....<BR>
